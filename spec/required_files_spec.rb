require 'spec_helper'

describe RequiredFiles do
  it 'has a version number' do
    expect(RequiredFiles::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).not_to eq(true)
  end
end
